# Handoff verification contract

Exact-producer mode is the fail-closed default and requires an explicit producer repository or deprecated explicit environment alias. Vendored-only mode is deliberately limited and never reports full handoff. No-physics is outside this verifier.
