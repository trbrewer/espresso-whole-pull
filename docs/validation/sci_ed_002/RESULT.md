# Current SCI-ED-002 result

The protocol remains scientifically blocked because the reference-extractability stopping rule is not defensibly frozen. Owner closeout does not close that method gap or authorize commissioning.

The EWP consumer is technically qualified for merge as a valid blocked research artifact. The accepted final suite ran 987 tests: 985 passed, two were documented skips, and there were zero failures or errors. Source integrity, static validation, exact-producer verification, hosted CI, and branch-scoped no-physics verification passed. The unchanged historical verifier still reports its pre-existing `openfoam_solver_source` mismatch; it did not pass, and its identical base/candidate fingerprint is not branch-diff evidence.

The closed v3 contract classifies all 82 authority fields and verifies the exact Puckworks commit, tree, export, both manifests, all twenty schemas, SCI-MD-007, historical provenance, and the claim ceiling. Vendored-only mode remains limited. The prior R2D result—`SCI_ED_002_R2D_COMPLETE_NEGATIVE_CONTROL_COVERAGE_NOT_ESTABLISHED`—remains historically true: the public matrix does not exhaustively demonstrate every historical-provenance and manifest-corruption combination through the complete CLI dispatcher.

The owner accepts that residual gap as `SCI_ED_002_NONBLOCKING_TECHNICAL_DEBT_EXHAUSTIVE_PUBLIC_CLI_NEGATIVE_CONTROL_MATRIX_INCOMPLETE`. It is nonblocking because it identifies no known incorrect producer authority, accepted invalid mutation, production defect, governing-physics change, or scientific result defect. Immediate remediation and further exact-head rereview are waived. Merge is authorized only to preserve a valid scientifically blocked research result; commissioning remains unauthorized.
