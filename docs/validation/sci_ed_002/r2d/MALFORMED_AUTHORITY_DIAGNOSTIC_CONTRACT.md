# Malformed-authority diagnostics

Expected malformed JSON, type, identity, hash, path, collection, and closed-field errors cross the CLI boundary as structured nonzero failures. Output is invalidated before evaluation and atomically replaced; expected and observed sources are named explicitly.
