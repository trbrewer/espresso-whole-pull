# Negative controls

Every recorded control maps to a public unittest node and the dedicated hosted workflow executes the complete module. Parameterized subtests cover all four claim-mutation classes, current pins, malformed structures, same-tree commit substitution, stale-output replacement, deprecated fields, and limited vendored semantics.
