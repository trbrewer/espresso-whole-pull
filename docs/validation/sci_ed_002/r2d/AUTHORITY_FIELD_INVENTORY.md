# Authority-field inventory

The v3 current lock contains 82 recursively enumerated leaf fields: 70 adjudicative current authorities, 5 derived informational fields, 7 historical-provenance fields, and zero retained deprecated duplicates. The JSON inventory records every JSON pointer, classification, and value type. Closed-field validation rejects unknown or reintroduced legacy fields.
