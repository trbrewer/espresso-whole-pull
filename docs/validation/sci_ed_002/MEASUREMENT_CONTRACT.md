# SCI-ED-002 measurement-contract handoff

This is a thin consumer of the exact Puckworks candidate, not a competing protocol. `T_total` means method-conditioned total assayable roasted content; `I_ref` means separately method-conditioned operational reference-extractable inventory. They are not interchangeable and neither is production-shot extraction, `Q_production_solid_initial`, or `c_s0`. The production quantity and mapping remain `NOT_ESTABLISHED`.

The optional espresso extension binds Puckworks material–roast and pre-brew aliquot IDs to the existing EWP shot metadata, synchronized time series, fraction metadata, chemistry, calibration, exclusions, and file-manifest contracts. Reference extraction remains distinct from shot extraction. No production code, runtime default, solver parameter, OpenFOAM dictionary, or case is changed.

Status: candidate contract only; commissioning not authorized; additional direct measurements required; SCI-MD-007 must not be rerun until real VG01–VG05 evidence exists; predictor development is ineligible; VG06 remains sealed.
