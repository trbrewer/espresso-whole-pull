# SCI-MD-004 Stage A contract

This owner-authorized, `NO_GOVERNING_PHYSICS_CHANGE` stage freezes the cross-repository Angeloni data contract, an indexed passive-species design, exact one-species reduction, provenance rules, verification matrix, and protected one-shot future comparison. It implements no solver behavior.

All 66 beverage rows are `PROTECTED_EXTERNAL_NO_RETUNING_ENDPOINT_HOLDOUT`; `PREEXISTING_EXPOSURE = TRUE`. From this freeze onward they cannot select species, parameters, mappings, thresholds, hydraulic mappings, assumptions, or variants. Inspection followed by retuning permanently reclassifies Angeloni as training/comparison data.

The primary disposition is `GO_STAGE_C_CONDITIONAL_HYDRAULIC_INPUT`: geometry, gauge pressure, temperature, nominal dose/yield, and grind-specific duration exist, but pressure/flow histories and an independent grind-to-permeability map do not. Angeloni fitted K(p) is forbidden. A future comparison is therefore conditional on predeclared nonchemical hydraulic/apparatus observations.

The normative machine-readable contract is `validation/contracts/SCI_MD_004_STAGE_A_MULTISPECIES.json`. Physical validation remains `NOT_ESTABLISHED`.
