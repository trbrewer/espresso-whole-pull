# VAL-001 schema-provenance and semantic-enforcement correction

This additive PR #38 correction replaces relabelled inferred schema provenance,
anchors semantic profiles outside mutable record content, materializes every
claimed mutation, and derives schema counts from one registered taxonomy.

It preserves the original result, failed invocation, V2 result, four-event
journal, invocation summary, consumed execution state, locked Puckworks
identity, and external OpenFOAM evidence. It authorizes no comparison,
OpenFOAM run, fitting, protected access, experiment, or governing-physics
change. Physical validation remains `NOT_ESTABLISHED`.
