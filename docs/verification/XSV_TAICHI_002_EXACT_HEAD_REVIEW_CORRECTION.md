# XSV-TAICHI-002 exact-head review correction

## Authority and immutable lineage

- Authorization: `XSV-TAICHI-002-EXACT-HEAD-REVIEW-CORRECTIONS-2026-08-05`
- Pre-review head: `634cff4b2ea1c3adce9cb307ec6ded48bd134252`
- Pre-review tree: `f48034b5f32c05e0903bead9e623eb8b747535c0`
- Pre-review result: `f4d2cd03bb794ac89e2aba0ddbb133e8ed531d14dc5fb41d7e4a009197259236`
- Historical execution runtime: `3bbf089ab5855bdbaeabb9a569ec9176974e8c25499a0c43c0d011be69d74a75`
- Corrected formula: `XSV_TAICHI_002_REVIEW_REDUCER_V2`

The historical runtime remains byte-identical. The correction reducer did not
execute CUDA or another numerical model; it read and qualified the existing 22
primitive records and immutable external package. The target, target inputs,
case matrix, geometry manifest, masks, run records, raw manifest, and archive
are unchanged.

## Corrected derivation

G0 through G6 local-package dispositions are now calculated from named
evidence, hashes, primitive fields, and validation rules. The result contains
the complete check inventory and failure reason for every gate. Exact-head CI
is an external review-time observation and remains `PENDING` in committed
records.

Every run is independently bound to the case matrix and frozen contracts by
run/order identity, geometry and payload, direction and transpose, force label
and value, `tau_plus`, precision, CUDA architecture, convergence settings,
Puckworks commit/tree/source hashes, mask shape/dtype, and primitive artifact
hashes. The package establishes:

```text
ALL_22_FROZEN_IDENTITIES_EXECUTED
FINAL_PACKAGE_ORDER_MATCHES_FROZEN_CASE_MATRIX
CHRONOLOGICAL_EXECUTION_ORDER_NOT_INDEPENDENTLY_RECONSTRUCTED
```

No separate immutable chronology ledger was retained, so the package does not
claim independent reconstruction of wall-clock execution order.

## Corrected scientific disposition

The primary apparent-conductance ratio target is exactly
`0.37327310642080013`. The separate nominal-pressure ordering screen is
`0.4545454545454545`. X-direction coating ratios are approximately `0.7787`,
`0.6134`, and `0.4241` for C05, C15, and C30. C30 crosses only the nominal
screen.

- Constriction:
  `REQUIRED_COLLAPSE_NOT_ATTAINED_WITHIN_SCREENED_CONSTRICTION_ENVELOPE`
- Heterogeneity:
  `REQUIRED_COLLAPSE_NOT_ATTAINED_WITHIN_SCREENED_HETEROGENEITY_ENVELOPE`
- Localization:
  `FLOW_LOCALIZATION_RESPONSE_REPORTED_DESCRIPTIVELY_NO_PROSPECTIVE_CHANGE_THRESHOLD`
- Anisotropy: `DIRECTIONAL_PERMEABILITY_RESPONSE_REPORTED_DESCRIPTIVELY`
- Overall X direction:
  `REQUIRED_COLLAPSE_NOT_ATTAINED_WITHIN_SCREENED_X_DIRECTION_ENVELOPE`

Heterogeneity attainment is evaluated independently at amplitudes 1 and 2
using each amplitude's three paired seeds. Counts are never pooled to claim
three-of-three robustness. No prospective localization-change threshold was
frozen, so exact sigma, coefficient-of-variation, fastest-quartile-share, and
paired values are descriptive only.

C30 direction-normalized permeability ratios are:

- X: `0.424082396862255`
- Y: `0.17610126503036505`
- Z: `0.1937468574408694`

The Y and Z ratios are below the primary numerical target relative to their
corresponding directional baselines. These descriptive anisotropy anchors do
not alter the primary X-direction disposition or establish real-coffee
anisotropy. They strengthen only the conditional case for future directional
permeability or fabric measurement.

## Plots and claim boundary

The corrected package has ten SVG files implementing eight required plot
groups. Every K/K0 plot distinguishes the primary apparent-conductance target
from the nominal-pressure ordering screen. Non-K/K0 plots carry both
definitions in metadata rather than drawing dimensionally inappropriate
lines. The source table records target values/types/source, normalization,
direction, case identity, and qualification state.

This remains an exact static synthetic screen. It establishes no real-coffee
morphology, compaction, fines, clogging, channeling, mechanism, or physical
validation. `ADDITIONAL_INDEPENDENT_DATA_REQUIRED` remains the current gate.

## G8 fail-closed evidence binding

The G8 correction preserves reducer v2 and the historical CUDA runtime while
adding reducer v3 as current reduction authority. Git object inspection binds
the final G0 head `df50ec4be2734e26aa91715d3c27009ad32d0cc1`, historical G0 protocol hash
`4f3d6a528620b3d9d1d9ce39b3b9f088deb37586d33b3d761f690049610a3d7c`,
and distinct current reporting protocol hash
`04911d266c77470f7d7a83a39842090100407a43fc3a36990b2177eea5496c28`.
Both required historical checks were recovered at the exact G0 head.

Reducer v3 rederives all three target ratios and verifies both retained mask
copies for all twelve geometries, including counts, porosities, periodic
winding connectivity, and coating removal sets. It retains 22 successful run
records. No independent immutable process-attempt ledger exists, so process-
attempt count, retry-ceiling compliance, and chronological execution order are
`NOT_INDEPENDENTLY_RECONSTRUCTED`; package ordering is not represented as
launch history. This provenance limitation does not disqualify the retained
numerical results or change the X-direction non-attainment disposition.

## G9 final package-integrity closure

Reducer v4 is the fail-closed evidence-binding wrapper around the unchanged
v2 scientific reduction. It verifies the frozen protocol, matrix, target,
geometry manifest, and historical-runtime bytes; the exact ordered and unique
22-run set; each prohibited-claim occurrence in its own context; and the
complete deterministic committed-output inventory. Independent clean output
A and B inventories must be byte-identical and must match the committed core.
The G3 process-attempt provenance limitation remains explicit. No numerical
execution or scientific-evidence change occurred, and exact-head CI remains a
review-time observation rather than a claim embedded in the candidate commit.
