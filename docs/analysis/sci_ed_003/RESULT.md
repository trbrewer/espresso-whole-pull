# SCI-ED-003 result

## Disposition

`SCI_ED_003_MINIMUM_DECISION_RELEVANT_CLOSURE_CONTRACT_DEFINED_SEPARATE_EXECUTION_AUTHORIZATION_REQUIRED`

A Pareto-minimal staged candidate is defined: M01 paired absolute chemistry and mass closure plus the contextual subset of M02 synchronized shot telemetry. It addresses the repeated absolute-inventory/closure blocker and supplies the shot-specific boundary/clock needed to interpret those same fractions. Each included measurement fails at least one named decision when removed. No lower-cost eligible existing-data route supplies the same information.

Stage F would be nonadjudicative method/tail/recovery/pairing/closure/variance feasibility. Stage D would be separately authorized and sized/frozen only after Stage F supplies empirical variance, covariance, tail, and capability inputs. Neither stage is authorized here.

M03 is deferred until porosity/permeability uncertainty can change a named EWP output decision and a source-property-to-effective-EWP bridge is plausible. M04 is deferred until a genuinely distinguishing synchronized intervention exists; fixed resistance remains retained by parsimony. M05 is later independent transfer after local qualification and a separate G1/G3 freeze.

## Scientific boundary

`T_total`, method-conditioned initial/spent `I_ref`, absolute cup/fraction/retained mass, `Q_production_solid_initial`, and `c_s0` remain distinct. The operational closure retains every declared term and covariance; no unobserved term is zero by assumption. `I_ref_EQUALS_PRODUCTION_M0` and `c_s0_MAPPING` remain `NOT_ESTABLISHED`.

The SCI-ED-002 1% / two-consecutive / eight-cycle proposal is not adopted. Stage F retains all sequential fractions, blank/LOD/LOQ/censoring/recovery/calibration records, estimates cumulative and tail mass separately by analyte, and returns no qualified endpoint if a quantified or material tail persists at the feasible maximum. Exact endpoint and counts remain `PILOT_REQUIRED`, `DESIGN_CALCULATION_REQUIRED`, or `OPERATOR_CAPABILITY_INPUT_REQUIRED`.

The independent shot unit is the independently ground, independently prepared puck/shot. Fractions, cycles, preparations, and injections are nested or technical replicates. No numeric sample count, sensor tolerance, rate, or decision threshold was invented.

No laboratory observations entered and no experiment, procurement, contact, OpenFOAM/model execution, model decision, physics/default/parameter change, inventory predictor, or mapping was performed. Physical validation remains `NOT_ESTABLISHED`; the claim ceiling is `CLOSURE_CONTRACT_ONLY`.
