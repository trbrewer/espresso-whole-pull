# SCI-MD-RHEOLOGY-007 autonomous parallel paths

G1 / SOURCE_SCENARIO_CHANGE_ONLY. Owner-authorized issue #161, one isolated
branch and focused PR. Starting fetched main/accepted PR #160 merge
`e5b3ef819f9692973f674d5da52511d3b3dad07c`, tree
`c930fab51b530e20d48efdde9e2bfda4e0cba118`; #159 closed. No drift.
Current minimum governance applies; historical stage controls have expired.
One independent exact pre-scoring audit, then ordinary PR protection. No second
scientific review ladder. C is exposed computational reference, not experimental
truth or protected holdout. No fitting, production changes, dependency advancement,
Puckworks writes, adoption, laboratory work, merge or automatic successor.

NEW_INFORMATION: an autonomous, parameter-free spatial reduction has not been
tested by a bulk-state closure or uniform scalar-invariance theorem.
DECISION_IF_SUFFICIENT: prefer the cheaper representation for qualified outputs
and envelope in subsequent proposals only; no default adoption.
DECISION_IF_INSUFFICIENT: retain multidimensional coupling for failed outputs;
reject this non-exchanging construction, not all reduced models.
DECISION_IF_UNRESOLVED_OR_BLOCKED: no promotion; identify numerical, support,
execution or provenance limit; no rescue sweep.
GRINDER_TO_CUP_LINK: heterogeneous water allocation and aggregate delivery.
LOWER_COST_ALTERNATIVE: compose existing uniform EWP runs without another PDE.
REPEATED_BLOCKER: this is not species inventory, physical transfer or the stopped
E2C lane. Industrial-source transfer and independent validation are not scored.

## Source and available-data preflight

Accepted 003 SOURCE_USE/EXPORT, 004 observer, 005 protocol/result/property and
006 amended protocol/result/schema/reuse/compatibility supply authority. External
006 location is verified against its frozen artifact-root hash, accepted closeout
and case manifests; no guessed evidence or summary substitutes. All 18 science
case manifests and full interval traces are verified. Analysis Puckworks commit
2058d0e947ee9eb92c52d64f6165b810f1fb4732, tree
a6ffb312473b15be43c1571a893b19873ea47c5a is distinct from runtime lock
fc61c4670ec7bf801e40bb391aab16048b8da26b. Existing data authority, manifest,
capability register, cards and accepted source hashes provide scoped sufficiency;
no new source, corpus search or general data-gap claim. brewer2026/streamtube.py
is architecture context only: no Cameron constants, fitted distributions,
fines evolution, solver imports or output claims enter P.

## Mathematical construction and frozen envelope

For an isolated transversely uniform tube of constant area A, each pressure,
storage, axial advection/diffusion and extraction balance has a common factor A.
Dividing by A preserves intensive equations; fluxes and inventories scale with A.
Each native constituent retains the full axial concentration and inventory states,
local beginning-step viscosity update and independent pressure solve. No dynamic
state from C or the other path is available to the runner. This construction is
a distinct reduced model, not an equation equivalence to communicating radial C.
P has no transverse dynamics; therefore hold P_base against C_radial when measuring
radial sensitivity, subject to the six area/dimensional fixtures.

Use full-basket R=.029 m, L=.009011660896432553 m, dose .020 kg, phi=.4,
inventory .0056 kg, capacity 180 kg/m3, rate .15/s, D=1e-9 m2/s, rho=965 kg/m3,
T=363.15 K; saturated, c=0, static k/phi, constant 3 or 9 bar, no ramp,
0–30 s with no target stop. Exact accepted dictionaries are regenerated and
compared completely. Changes only identifiers, output cadence, declared mesh/time,
and replacing radial permeability with uniform inner 3e-15 or outer 7.5e-16 m2.
Inactive wetting k stays unchanged. Exactly TR_LINEAR and SW_WATER_ANCHORED_90C,
with unchanged accepted tables and separate SW refined table. TR representation
is exactly accepted piecewise linear; its property allowance is zero.

Weights .25/.75 apply exactly once to extensive interval increments before sums,
cumulatives or ratios. Represented dose remains 20 g; inventories 1.4/4.2 g.
Q_P=.25 Q_i+.75 Q_o; s_P=.25 Q_i/Q_P; W/S similarly; B=W+S.
No intensive averaging (TDS, mu, c, extraction fraction). Mixed TDS=100 delta S /
delta B. Regional states remain regional. A changing flow split emerges from
independent viscosity histories. The 4/7 split is only a constant-viscosity test.

## Execution and qualification

28 full native slots: TR 2 pressures x 2 constituents x (base,temporal,axial)=12;
SW adds property=16. Base 512x2 dt=.02; temporal 512x2 dt=.01;
axial 1024x2 dt=.02; property base with refined SW table. Final fields retained.
Exact accepted executable is available and hash verified: select ZERO bridge
runs before freeze. If unavailable before selection, freeze a four-base-C bridge:
flow relative <=1e-8, mass/storage/inventory <=1e-12 kg, share <=1e-8 absolute,
and inherited clocks/schema/balance gates; no max(1,SI reference) normalization.
Failed bridge blocks mixed-executable reuse; no C campaign regeneration.
At most four nonsemantic full recovery attempts; absolute ceiling 36 including
failed/bridge attempts. STARTED fsynced before launch; every failure retained;
completed hash-valid slots reused on resume; no overwrite. No scientific changes
in recovery. Durations/cell counts are descriptive, no benchmark campaign.

Six short TR/9 bar .2 s slots, dt=.02, 32 axial: inner/outer full-basket at 2
radial, each area counterpart with R scaled sqrt(f), dose scaled f, and both
full-basket cases at 8 radial. Geometry metadata stays valid. Smaller circles
are scaling fixtures, not annulus reconstructions. Check generated depth/area/
volume/inventory, intensive and area-normalized extensive histories, pressure
and concentration radial uniformity at equal axial coordinates; inspect full-run
final fields too. Relative tolerance 1e-6; denominator floors: Q 1e-14 m3/s,
volume 1e-16 m3, mass 1e-12 kg, c 1e-8 kg/m3, p 1e-3 Pa, mu 1e-12 Pa s.
Floors bound near-zero roundoff only and are not max(1,SI) scaling. At most two
correction repeats of the same short slots, no pilot. Mapping failure is a
qualification blocker, not scientific inadequacy. Reuse 006 analytical constant
viscosity/property verification. Focused synthetic tests qualify weighting,
closure, permutation, equal paths, analytical Q/split, conservative first-fraction
splitting, invalid clocks/increments/nonfinite/double scaling and decisions.

Native C uses accepted radial validator/schema. Uniform constituents use accepted
uniform validator and 005 qualification, supplemented by exact clock matching and
1e-12 s duration agreement. P is explicitly derived, never a native 006 trace.
No invented series-resistance fields. Every interval including the first is used;
no hydraulic interpolation, smoothing, crop or sparse trapezoids. Reuse 004 compare
only after native validation, on conservative increments and zero-mass origin;
compare(P,C) maps its C label to P and R label to reference C. No old decision helper.

Inherited 005/006 gates: water/solute balance <=1e-8 kg, corrections sum abs <=1e-10
kg, c>=-1e-10 and <=180+1e-8 kg/m3, w<=.24, inventory>=-1e-12 and
<=initial+1e-10 kg, storage>=-1e-12 kg; Qdt-volume <=1e-15 m3;
water increment-rho Qdt <=1e-14 kg; positive Q, valid shares, finite contiguous
0–30 s; matching clocks exact, endpoint 1e-9 s, interval duration 1e-12 s.
Validate constituents before mixing; independently reconstruct composite water and
solute balances, zone sums and initial inventory. No cancellation of invalid arms.
Pressure/density conventions unchanged. Restricted transverse_abs_internal_flux
is not a score, correction, allowance, complete exchange or provenance measure.
C outlet-zone solute is not initial-region provenance. Any discrepancy can reflect
lateral pressure, transverse advection/diffusion and radial structure jointly;
these mechanisms are not individually identified.

## Support, six metrics and decisions

Before discrepancy scoring write SUPPORT.json per pressure: B_ref=min final B
across both laws and all C resolutions; B_star=min across all required C and P;
coverage=B_star/B_ref. Same B_star for every comparison including refinements.
Report B_star/each individual C endpoint too. Delivery qualification requires
coverage>=.95 and complete evidence. Shortfall is unresolved, not physical FAIL;
hydraulic/allocation decisions remain separately admissible. No extrapolation,
longer run, law-specific endpoint or altered five fractions.

| Metric | Definition (C denominator) | Budget |
|---|---|---:|
| E_Qint | sum(abs(QP-QC)dt)/sum(QC dt) | .01 |
| E_Qpeak | max(abs(QP-QC)/QC) | .02 |
| D_share_mean_pp | 100 sum(QC abs(sP-sC)dt)/sum(QC dt) | 1 pp |
| D_share_peak_pp | 100 max(abs(sP-sC)) | 2 pp |
| E_Spath | max_B abs(SP(B)-SC(B))/SC(B_star) | .01 |
| D_TDS_pp | max of abs(100 delta SP/delta B - 100 delta SC/delta B), five equal-B fractions | .10 pp |

Cumulative-solute extrema use union of both native mass breakpoints plus endpoints.
These are reduction budgets, not empirical error or physical-validation thresholds.
For every metric: u_time=abs(M(Pt,Ct)-M(Pb,Cb)); u_axial analogously;
u_radial=abs(M(Pb,Cr)-M(Pb,Cb)); SW u_property=abs(M(Pp,Cp)-M(Pb,Cb)),
TR zero; u_arithmetic=max discrepancy against independent higher-precision reduction
(including composition and cumulative sums, verify >53 mantissa bits).
u_total=sum five terms once. No inherited scalar or 1-D operator allowance.
Empirical sensitivities only, not confidence intervals or rigorous continuum bounds.
All gates and u_total<=.2 budget required. PASS iff value+u<budget; FAIL iff
value-u>budget; equality/straddling/missing/invalid/underqualified => UNRESOLVED.
Report 24 decisions. Qualified FAIL takes precedence over unresolved siblings.
Overall PARALLEL_PATH_REDUCTION_INSUFFICIENT if any qualified FAIL;
SUFFICIENT_FOR_TESTED_OUTPUTS suffix only if all pass with full delivery support;
otherwise UNRESOLVED, or BLOCKED_<SPECIFIC_CAUSE> for inadmissible execution/mapping.
Separate allocation, hydraulics and aggregate-delivery verdicts.

No internal-field equivalence, global minimality, portability, real espresso,
taste or default-readiness claim. TR dilute continuation, SW water anchoring and
90 C extrapolation, industrial/reconstituted-source transfer limits retained.
PHYSICAL_VALIDATION: NOT_ESTABLISHED. Accepted 001–006 findings unchanged.
