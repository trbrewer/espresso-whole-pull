# SCI-MD-RHEOLOGY-008 frozen communicating radial family

G1 / SOURCE_SCENARIO_CHANGE_ONLY. Current owner instruction authorizes this task
through an unmerged reviewable PR. Current minimum governance supersedes expired
stage ceremony. No production equation/operator/interface/default change, fitting,
closure coefficient, Puckworks write, dependency update, laboratory work, merge,
release, adoption or automatic successor. Production semantic change is a scope
conflict, not an invitation to G2. C is an exposed source-conditioned computational
reference, not experimental truth or a protected holdout.

Live fetched main is 7bd046eb1697b7ca0a6869c39098efab6f31f3c7, tree
63390194b70503135f3e1bcace8707fb1d38f02d, identical to accepted 007 head
613b868538126b5ae47b110b45a764c10fc9d162's tree. PR #162 is merged; initial
008 issue/PR search returned none. Isolated worktree preserves unrelated work.

NEW_INFORMATION: fixed 2/4/8-cell native communicating radial models have not
been tested by the autonomous, non-exchanging 007 P or bulk scalar constructions.
POSITIVE: identify the coarsest passing member among these three for a future
proposal only. NEGATIVE: reject all-output sufficiency of affected candidates,
not every reduced model. UNRESOLVED/BLOCKED: identify missing qualification,
support or evidence without tuning, extension or promotion. GRINDER_TO_CUP_LINK:
water allocation and beverage-mass-conditioned aggregate solute delivery.
LOWER_COST_ALTERNATIVE: P already passes hydraulic/allocation budgets but fails
one delivery budget; its existing traces supply the baseline without new P runs.
REPEATED_BLOCKER: this does not reopen stopped inventory or E2C research.

## Read-only evidence and scoped availability

Use 003 SOURCE_USE/EXPORT, 006 protocol/result/code and 007 protocol/result,
SUPPORT, REUSE, COMPATIBILITY and full artifacts. Locate C and Puckworks through
accepted P LOCATIONS.json, verify frozen root hashes, manifests, all 18 C science
traces and 28 P native constituent traces (14 compositions). Verify required C
case files, P file manifests and ledger; reconstruct every P/C comparison variant
as a regression, without running/modifying P. Summary tables never replace traces.
No broad corpus search. Accepted cards, source identities, manifest and capability
register suffice for this source-conditioned question; no new data-gap claim.

Analysis Puckworks is 2058d0e947ee9eb92c52d64f6165b810f1fb4732, tree
a6ffb312473b15be43c1571a893b19873ea47c5a. Production lock remains distinct at
fc61c4670ec7bf801e40bb391aab16048b8da26b. Accepted native executable SHA256 is
bea2860f92f4934c3f191b7da8d9c425f73ac254cd42131172dad133dd77d7ad.
Verify source, build, runtime/library and table identities. Missing accepted
executable/evidence blocks execution; no bridge, new C or new P campaign.
Restricted source tables, binaries, fields, private paths and full logs stay external.

## Fixed family and physical construction

C: 006 resolved reference, 64 radial base and 128 radial refinement.
P: accepted 007 two autonomous axial paths, weighted exactly once .25/.75.
E2/E4/E8: unchanged native wedge solver, unit radial grading, respectively
2/4/8 cells per axial station; same physical core/annulus interface. E2 has one
cell per zone; E4/E8 retain within-zone detail. This is a discrete spatial family,
not a derived exchange closure. All three are selected before any new scores.
Native E already contains physical radial areas: never reweight E as P.
Actual annular volumes, not equal cell weights, determine regional aggregation.
Verify interface faces, owner/neighbour connectivity and physical areas/volumes
from generated polyMesh. Full-cylinder scale is 2*pi/sin(wedge angle), preserving
.25/.75 zone fractions. No C/P evolving state supplies forcing to E.

R=.029 m, L=.009011660896432553 m, interface=.0145 m, phi=.4, dose=.020 kg,
initial inventory=.0056 kg, inner k=3e-15 m2, outer k=7.5e-16 m2, capacity=180
kg/m3, extraction=.15/s, diffusion=1e-9 m2/s, rho=965 kg/m3, T=363.15 K.
Saturated c=0 initial bed, fixed k and phi; constant 3/9 bar, zero ramp, 0–30 s,
no beverage termination. Accepted TR_LINEAR and SW_WATER_ANCHORED_90C tables,
including separate SW refined table, unchanged. Beginning-step w=c/(rho+c),
local viscosity, pressure solve and pressure-matrix darcyFlux; conservative native
advection/diffusion/extraction/storage, boundary/density/inventory conventions.
No U dot Sf substitution, numerical scheme/tolerance changes or generator changes.

For each N and pressure: base 512xN dt=.02; temporal 512xN dt=.01; axial
1024xN dt=.02. SW additionally property base settings/refined table. All 30 s.
TR 18 + SW 24 = 42 fixed science slots. Full attempts <=46 including failed
launches, <=4 nonsemantic recovery attempts. No larger N, pressures, duration,
geometry or parameter rescue. STARTED is fsynced before each launch; failures
and qualification failures remain. Resume only complete hash-valid artifacts;
no overwrite. Final fields, scenarios, native traces and manifests retained.
Use accepted tolerant final-directory selection (unique numeric time within 1e-9).
Cell counts/times are descriptive, not a controlled speed benchmark.

## Short qualification frozen before its launches

12 slots at 32 axial, .2 s, dt=.02; <=2 nonsemantic correction repeats, ceiling 14.
For each N: constant-water, doubled-constant viscosity, uniform permeability with
water viscosity. Synthetic constant tables only here; Q=pi R2(.25 ki+.75 ko) dp/
(mu L); heterogeneous share=4/7, uniform share=1/4; double mu halves Q. Three
E2 dynamic fixtures use accepted synthetic evolving table [mu,2mu,4mu] at
w=[0,.1,.24]: serial, identical repeat, radial MPI counterpart. No outcome pilot.
PREPARATION.json binds these configurations, executable, tables and tooling.

Mesh tetrahedral-volume reconstruction uses oriented native faces and annular
geometry; verify positive volumes, dose and inventory, face-aligned interface,
correct zone sums and final concentration/inventory volume integrals. Dynamic
fixture must show native nonzero signed interface advection (absolute water
flux >1e-18 m3/s and positive absolute upwind solute flux), using owner/neighbour
orientation; MPI places the material interface between ranks. Restricted
transverse_abs_internal_flux_m3_s is excluded, unchanged. Advective-only exchange
is not total advective-plus-diffusive transport or unique mechanism attribution.

Analytical Q/share and inverse-mu relative tolerance 1e-6; geometry relative 1e-8;
face alignment absolute 1e-12 m. Serial/repeat/MPI comparison relative 1e-6 with
floors: flow 1e-14 m3/s, volume 1e-16 m3, mass 1e-12 kg, viscosity 1e-12 Pa s,
other state/dimensionless values 1e-8 in their declared units. Balance cancellation
residuals use inherited absolute native gates separately, not a relative error; repeat also absolute
1e-12. No max(1,SI quantity) normalization. Native and independent balances apply
separately. Final native-field mass aggregation absolute tolerance 1e-10 kg.

## Native admissibility before scores

Reuse accepted radial validator plus independent native accounting. Water/solute
residual <=1e-8 kg; absolute correction sum <=1e-10 kg; c in [-1e-10,180+1e-8]
kg/m3, w<=.24, positive mu; inventory >=-1e-12 and <=initial+1e-10 kg;
storage>=-1e-12 kg; Qdt-volume <=1e-15 m3; water increment-rho Qdt <=1e-14 kg.
Positive finite total Q, valid shares, regional sums and initial inventory.
Contiguous 0–30 s intervals, endpoint tolerance 1e-9 s, duration 1e-12 s.
Comparison clocks match exactly. Failures make affected comparisons unresolved,
not physical reduction failures. Independent balance reconstructs native cumulative
water, inventory, dissolved storage, delivered solute and inlet loss.

## Fixed support, metrics and decisions

Copy exact pressure-specific B_star from accepted 007 SUPPORT.json before scores.
Never replace it with a candidate minimum. Retain original common-reference and
individual C coverage; these endpoints do not cover every reference whole shot.
All laws/N/variants use the same pressure endpoint. Insufficient E support makes
only affected delivery comparisons unresolved; no extrapolation/extension/crop.
B=W+S from conservative increments with zero origin. Five equal-B fractions fixed.

| Metric | Definition, E compared with C | Budget / unit |
|---|---|---|
| E_Qint | sum(abs(QE-QC) dt)/sum(QC dt) | .01 fraction |
| E_Qpeak | max(abs(QE-QC)/QC) | .02 fraction |
| D_share_mean_pp | 100 sum(QC abs(sE-sC) dt)/sum(QC dt) | 1 pp |
| D_share_peak_pp | 100 max(abs(sE-sC)) | 2 pp |
| E_Spath | max_B abs(SE(B)-SC(B))/SC(B_star) | .01 fraction |
| D_TDS_pp | max five fractions abs(100 deltaSE/deltaB - 100 deltaSC/deltaB) | .10 pp |

Use all native intervals for flow/share. Cumulative path extrema use union of
native beverage breakpoints plus endpoints. Exact conservative splitting at each
mass boundary; no concentration/TDS averaging, smoothing, omission of first
interval or sparse trapezoids. Independent 50-digit Decimal implementation takes
two native radial histories directly; it recomputes core shares, cumulative
increments, breakpoints and fraction splits. Inherited 1e-14 kg endpoint-roundoff
floor is arithmetic only, never physical support extension.

Per candidate/case/metric: u_time=abs(M(Et,Ct)-M(Eb,Cb)); u_axial analogously;
u_Cradial=abs(M(Eb,C128)-M(Eb,C64)); SW u_property=abs(M(Ep,Cp)-M(Eb,Cb));
TR property zero. u_arithmetic=max float/Decimal discrepancy over every required
variant. Sum once. E2/E4/E8 differences NEVER enter allowances: these are the
primary reduction effect. Empirical sensitivities, not confidence intervals or
rigorous continuum error bounds. Hold E fixed for C radial sensitivity.

Qualified evidence/support and u<=.20 budget required. PASS iff M+u<budget;
FAIL iff M-u>budget; equality, straddling, excessive allowance, missing or invalid
evidence => UNRESOLVED. Exactly 72 decisions, separated into hydraulics/allocation/
delivery. Any qualified FAIL makes a candidate insufficient despite unresolved
siblings. All 24 PASS makes it sufficient; otherwise unresolved. Select lowest N
with 24 PASS as COARSEST_QUALIFIED_PASSING_CANDIDATE_AMONG_2_4_8, never globally
minimal/necessary; explicitly report lower unresolved candidates. If none passes,
distinguish every-candidate failure, remaining unresolved and execution/evidence
blockage. Nonmonotonic behavior is reported without tuning.

One independent pre-scoring audit binds the freeze; unavailable audit cannot be
self-approved. Then ordinary PR review/protections, no extra scientific ladder.
Accepted P conclusion PARALLEL_PATH_REDUCTION_INSUFFICIENT and its tested
hydraulic/allocation success remain valid. Joint differences involve pressure
communication, transverse transport and radial representation, not an identified
unique mechanism. TR dilute continuation, SW water anchoring/90 C extrapolation,
industrial/reconstituted-source transfer caveats remain. No internal-field
equivalence, fresh-espresso validation, taste, portability or default-readiness.
PHYSICAL_VALIDATION: NOT_ESTABLISHED.
