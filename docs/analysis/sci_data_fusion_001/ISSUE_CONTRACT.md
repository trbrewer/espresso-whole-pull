# SCI-DATA-FUSION-001 issue contract

Scientific question: do two or more independently qualified source lineages place semantically compatible constraints on the same canonical EWP component quantity or uncertainty term, and does any common support narrow an authorized EWP domain or output envelope?

Governance is G1, `SOURCE_CONSTRAINT_ANALYSIS_ONLY`, with claim ceiling `CROSS_CORPUS_COMPONENT_EVIDENCE`. This task makes no governing-physics, production-solver, default, calibrated-parameter, runtime-lock, calibration, holdout, OpenFOAM, or laboratory change. It performs no raw-row fusion or probabilistic meta-analysis and establishes no physical validation.

Authority is fetched EWP `origin/main`, the merged completed-task artifacts enumerated in `CONSUMED_RESULT_ARTIFACTS.json`, and only the exact Puckworks commit/tree and MANIFEST/register/index hashes in the live programme. Puckworks must be supplied explicitly and every identity must match; there is no filesystem, main-branch, installed-package, or network fallback.

The candidate universe comprises every support, boundary, uncertainty statement, operator, and exclusion in the consumed completed EWP artifacts plus a full metadata scan of the exact Puckworks family index and available-data register for potentially matching quantities. Unqualified families remain candidates or successors only. Every candidate receives one terminal role.

Compatibility requires the same physical quantity and compatible reference state, authorized units/basis, spatial and temporal support, observation operator or existing qualified bridge, population/regime, distinct lineage, independence/target-exposure role, provenance/rights, and EWP consumer, without new inference, fitting, reconstruction, or physics. Unknown load-bearing semantics fail closed. Repeated files, tables, figures, reductions, or fitted/measured outputs from one campaign do not multiply lineages.

All source supports and identities remain separate. No raw-row concatenation, cross-source empirical distribution, averaged quantiles, range-to-CI conversion, inverse-variance pooling, Bayesian updating, random-effects analysis, or likelihood multiplication is permitted. Same-scope compatible intervals may intersect; disjoint same-scope intervals conflict; differing regimes remain conditioned alternatives. No numerical uncertainty rule is frozen, so uncertainty statements cannot be numerically combined.

Quantitative EWP narrowing requires an explicit authorized machine-readable baseline; defaults, tolerances, arbitrary bands, source ranges, and unstated distributions are prohibited baselines. Optional propagation is limited to an existing qualified unchanged adapter and frozen materiality rule; no OpenFOAM is allowed.

Sentinels preserve separate Wadsworth/Vaca rows and operators; prevent assumed equivalence of total XCT porosity and Vaca epsilon_0; keep both permeability substitutions stress-only; keep Vaca Figure 12 operator-only; keep Visualizer at zero qualified EWP pressure boundaries; keep Pannusch at zero operational mappings and non-independent/target-exposed; and retain fixed resistance under the Waszkiewicz result.

Component results distinguish positive common constraints, exact compatible uncertainty support, common support without a numeric baseline, source-conditioned complementarity, negative, same-scope conflict, semantic/authority/rights blockers, and no shared component. The deterministic reducer preserves every component result; only a decision-material blocker controls the overall result.

Stop on stale predecessor/programme authority, exact Puckworks mismatch, unregistered source, prohibited rights, required new inference or physics, improper protected/consumed reuse, producer defect, protected production/default/runtime-lock mutation, or Phase B without one independent audit of the exact freeze. Negative and complementary results are successful outcomes.

Expected output is the complete artifact package, focused tests, deterministic replay, programme/current-state closeout, and one PR. Positive results lead only to a separately authorized consumer task; common support without baseline may lead to a bounded baseline contract if decision-material; complementary/negative results preserve separation and advance toward SCI-ED-003 when existing-data routes are exhausted; conflicts are preserved; blockers get at most one decision-material reconciliation.

Owner authorization: `SCI-DATA-FUSION-001-OWNER-AUTHORIZE-BOUNDED-CROSS-CORPUS-COMPONENT-CONSTRAINT-QUALIFICATION-2026-09-02`. Phase B is already owner-authorized after the independent freeze audit. No merge or self-approval is authorized.
