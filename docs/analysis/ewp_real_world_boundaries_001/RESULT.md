# EWP-REAL-WORLD-BOUNDARIES-001 result

## Disposition and scientific question

`EWP_REAL_WORLD_BOUNDARIES_001_BLOCKED` with stop code
`EWP_REAL_WORLD_BOUNDARIES_001_STOP_ALL_LOAD_BEARING_MEASUREMENT_MAPPINGS_UNRESOLVED`.
The permissioned recent-public Visualizer current-state corpus can be read and
schema-qualified, but the canonical normalized records provide neither a
documented device/pressure-sensor family nor resolved integration provenance.
The prospectively frozen transfer rule therefore admits zero EWP-executable
pressure boundaries. No aggregate boundary library or porosity-conditioned EWP
response map was manufactured.

## Governance, authority, and protocol

This is G1 `SOURCE_SCENARIO_CHANGE_ONLY`, with
`NO_GOVERNING_PHYSICS_CHANGE`, `NO_PRODUCTION_DEFAULT_CHANGE`, and
`NO_RUNTIME_PUCKWORKS_LOCK_CHANGE`. Starting EWP authority was merge
`34deac91170f49587820e60b6e357bfdc3e0874a`, tree
`e57f7318ffa02bfb5dc37f2a45646e7d6b3e569b`. The protocol was frozen before
the adjudicative current-state run; its commit, tree, and hash are recorded in
the PR and final execution report. No frozen rule was changed after result
inspection.

Pinned Puckworks authority was commit
`a3428a4d4ad571ef3168a70e8a04620fca5d3520`, tree
`6175b4ad39f45ebcdec32a176e5611bf3b03655b`. The verified MANIFEST,
available-data register, local-family index, and Visualizer-permission hashes
are recorded in `AUTHORITY.json`. No current-main or runtime-lock fallback was
used.

## Source state, rights, and privacy

`VISUALIZER_COFFEE_API_CRAWL_2026_07_15` was consumed only through
`CorpusSnapshot(classification="current-state").latest()`. It reconciled at
23,169 logical records and 23,169 stored versions, with zero revisions,
same-timestamp conflicts, missing update times, or quarantined records. Store
schema 6, bronze schema 1, normalizer hash, canonical manifest hash, and equal
start/end aggregate source-state hashes are in `CORPUS_SNAPSHOT_RECEIPT.json`.
No network or API path was used.

Visualizer is credited as the data source, and we collectively acknowledge the
users who make their shots public. Authority permits analysis of recent-public
API-accessible records, not an open-data-license claim, private-record use, raw
redistribution, per-shot publication, or public user linkage. Publication
thresholds were 20 shots and 10 linked contributors. Because the only device
cell was unresolved, it was suppressed; zero cells were published or coarsened.
The cohort is self-selected and recent-public, not representative of all users.

## Qualification and measurement mapping

All 23,169 records carried valid store-local pseudonymous linkage spanning 555
linked labels; these labels are store-local selection-bias accounting aids, not
real or cross-store identities. Primary equal-user weighting and the frozen
1,000-replicate user-cluster bootstrap were not run because no privacy-qualified
device cell survived the material stop. No linkage value or bootstrap membership
is committed.

Aggregate structural qualification found 14,448 records eligible for at least
one descriptive/library-only tier and 8,721 excluded from every task tier.
Overlapping eligibility counts and reason counts are reported in
`QUALIFICATION_COUNTS.csv`; they are not falsely summed as disjoint categories.
The row-complete private table remains outside Git.

Achieved and commanded pressure remain separate. Neither is transferable
without the missing device/controller and sensor-location authority. Native
flow remains ambiguous and excluded from SI use. Confirmed scale-derived flow
is an outlet mass-flow observation only, never an inlet boundary. Cumulative
scale beverage mass and water dispensed remain distinct. Source dose can affect
only the normalized target-ratio operator; temperature remains descriptive and
cannot alter material properties. No Visualizer quantity is treated as
permeability, and no pressure/flow fitting or dynamic-resistance selection was
performed.

## Library, R0, EWP execution, and materiality

Candidate cells: 0. Selected cases: 0. EWP-executable cases: 0. Library-only
published cases: 0. Coverage, low/central/high roles, R0 cohort position,
equal-user/shot weighting differences, bounded-sample sensitivity, the
three-state porosity matrix, numerical convergence anchors, and
porosity-by-boundary materiality are `NOT_ADJUDICATED` because doing so would
require silently assigning unresolved sensor/device semantics. The two accepted
porosity supports remain separate and unchanged. Saturated and wetting
permeability, R0, solver source, numerical method, and all production defaults
remain unchanged. Full OpenFOAM is
`NOT_REQUIRED_FOR_BOUNDARY_LIBRARY_AND_REDUCED_RESPONSE_MAPPING` and no full
anchor was run.

## Decision, claim, and successor

The machine-readable gates stop because exact authority, rights, privacy,
canonical source identity, source immutability, and protocol pass, while every
load-bearing pressure mapping is unresolved and the executable-case count is
zero. The claim ceiling remains
`RECENT_PUBLIC_COHORT_BOUNDARY_QUALIFICATION_AND_POROSITY_CONDITIONED_EWP_HYDRAULIC_RESPONSE_MAPPING`,
but no boundary-library or response-map claim was earned.

Exactly one bounded successor is selected and not implemented:
`EWP-RWB-001-PRESSURE-SENSOR-BOUNDARY-INTERFACE-RECONCILIATION`. It must establish
documented device/controller, pressure-sensor location, and integration-source
semantics without live corpus acquisition before this task can reopen. It is not
laboratory work.

## Reproducibility and invariants

The deterministic runner requires explicit Puckworks, store, private-work, and
output paths plus the exact protocol hash. Private paths, source filenames,
record IDs, user labels, timestamps, traces, raw manifests, and per-record
features are absent from committed evidence. No raw data, source permeability,
production-default change, governing-physics change, live acquisition, Puckworks
change, or laboratory operation occurred. Physical validation, population
representativeness, chemistry, causal inference, and machine validation remain
unestablished.
