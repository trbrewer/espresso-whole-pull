# Public v0.1.4 baseline summary

This directory contains compact sanitized metadata derived from the offline frozen WP-0.1H v0.1.4 baseline.

It does not contain reconstructed fields, an executable, processor directories, full qualification runs, or host logs. Original artifact hashes are retained for independent provenance review.

The baseline is numerically qualified for bounded R0. Physical validation is `NOT_ESTABLISHED`.
